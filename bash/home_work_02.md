1) Написать скрипт, который запросит у пользователя абсолютный путь к директории и проверит, существует ли она. Если существует - то выведет список файлов в ней.
Затем создаст в этой директории файл с именем по такому шаблону: `2026-03-28_project`.
2) Написать скрипт, который архивирует директорию, указанную ему через аргумент
3) Написать скрипт для проверки доступности списка URL, с уведомлением в TG при недоступности.
4) Написать скрипт, который будет проверять статус службы (передается в скрпит как аргумент) и если она НЕ запущена - запускать
5) Написать скрипт для тестирования SSL-сертификатов (openssl) с предупреждением об истечении
